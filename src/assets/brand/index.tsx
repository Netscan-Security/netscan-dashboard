import { CSSProperties } from "react";

export type BaseIconType = {
  size?: number;
  className?: string;
  style?: CSSProperties;
};

// export const NetscanLogo: React.FC<BaseIconType> = ({  }) => (

// )
